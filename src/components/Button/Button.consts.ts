import styles from './Button.module.scss';

export const baseClass = styles.root;

export const buttonSize = {
  sm: styles.sizeSm,
  md: styles.sizeMd,
  lg: styles.sizeLg,
};

export const stylesForButton = {
  primary: styles.primary,
  secondary: styles.secondary,
  danger: styles.danger,
};
